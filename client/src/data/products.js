// Category labels double as site navigation/filter chrome, kept static here
// rather than fetched — the actual product catalog now comes from the API
// (see services/productService.js); this file only backs static UI structure.
export const CATEGORIES = ['Outerwear', 'Shirts', 'Knitwear', 'Trousers', 'Dresses'];

export const SIZES = ['XS', 'S', 'M', 'L', 'XL'];
